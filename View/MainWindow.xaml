<Window x:Class="DigitalWellBeingApp.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:DigitalWellBeingApp.ViewModels"
        xmlns:comp="clr-namespace:DigitalWellBeingApp.View.Components"
        Title="Digital Well-Being"
        Height="450"
        Width="450">
    <Window.DataContext>
        <vm:AppTimeViewModel/>

    </Window.DataContext>
    <Grid x:Name="Main" ShowGridLines="True">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <StackPanel Margin="20,20,20,34" Grid.RowSpan="2">
            <TextBlock Text="App Usage Times"
                       FontSize="20"
                       FontWeight="Bold"
                       Margin="0 0 0 10"/>
            <comp:WeeklyUsageChart Height="183" Width="415"/>
            <ItemsControl ItemsSource="{Binding AppUsages}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <comp:AppUsageCard/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <!-- This will list out all apps from AppUsages -->
        </StackPanel>
    </Grid>
</Window>
